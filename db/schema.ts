import { sqliteTable, text, integer, index, uniqueIndex } from 'drizzle-orm/sqlite-core';
export const projects=sqliteTable('projects',{id:text('id').primaryKey(),owner:text('owner').notNull(),name:text('name').notNull(),prompt:text('prompt').notNull(),template:text('template').notNull(),status:text('status').notNull().default('draft'),created:text('created').notNull(),updated:text('updated').notNull()},t=>[index('idx_projects_owner_updated').on(t.owner,t.updated)]);
export const builds=sqliteTable('builds',{id:text('id').primaryKey(),projectId:text('project_id').notNull().references(()=>projects.id,{onDelete:'cascade'}),status:text('status').notNull(),digest:text('digest').notNull(),files:text('files').notNull(),report:text('report'),testingStarted:integer('testing_started'),created:text('created').notNull()},t=>[index('idx_builds_project_created').on(t.projectId,t.created),uniqueIndex('idx_builds_project_digest').on(t.projectId,t.digest)]);
export const events=sqliteTable('events',{id:text('id').primaryKey(),projectId:text('project_id').notNull().references(()=>projects.id,{onDelete:'cascade'}),agent:text('agent').notNull(),message:text('message').notNull(),created:text('created').notNull()},t=>[index('idx_events_project_created').on(t.projectId,t.created)]);
export const artifacts=sqliteTable('artifacts',{id:text('id').primaryKey(),projectId:text('project_id').notNull().references(()=>projects.id,{onDelete:'cascade'}),kind:text('kind').notNull(),content:text('content').notNull(),created:text('created').notNull()},t=>[index('idx_artifacts_project_created').on(t.projectId,t.created)]);
